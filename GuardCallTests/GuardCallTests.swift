import XCTest
@testable import GuardCall

final class GuardCallTests: XCTestCase {

    func testExample() throws {
        // A simple test to verify the test suite is working
        XCTAssertTrue(true)
    }

}
